apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-ingress
  namespace: jihonrado-web
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/tls-acme: "true"
spec:
  tls:
  - hosts:
    - jihonrado.es
    secretName: jihonrado-tls
  rules:
  - host: jihonrado.es
    http:
      paths:
      - path: /
        backend:
          serviceName: web
          servicePort: 3000
